{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LifeTreeAgentInputSchema",
  "description": "Schema for validating input to the LifeTree Agent",
  "type": "object",
  "properties": {
    "task_input": {
      "type": "string",
      "description": "Task to be performed by the LifeTree agent"
    },
    "project_id": {
      "type": ["string", "null"],
      "description": "Unique identifier for the project"
    },
    "temperature": {
      "type": "number",
      "description": "Temperature setting for response generation",
      "minimum": 0.0,
      "maximum": 1.0,
      "default": 0.7
    },
    "max_tokens": {
      "type": "integer",
      "description": "Maximum number of tokens in the response",
      "minimum": 1,
      "default": 1000
    },
    "memory_context": {
      "type": ["string", "null"],
      "description": "Optional memory context to include in processing"
    }
  },
  "required": ["task_input"]
}
